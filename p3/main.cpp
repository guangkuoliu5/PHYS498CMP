#include<iostream>
double Energy(vector<vector<int>> &config):
	w=config[0].size();
	h=config.size();
	for(int i=0; i<w; i++) {
		for (int j=0; j<h; j++) {
			cur=config[i][j];
			left=config[][j];


